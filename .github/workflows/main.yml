name: SPFx PipelineSolution1

on:
  push:
    branches:
      - CIPipeLineBranch
  workflow_dispatch:
    inputs:
      debug:
        description: 'Enable debug logging and upload transcript (true/false)'
        required: false
        default: 'false'

jobs:
  build-deploy:
    runs-on: windows-latest

    steps:
      # 🧩 Step 1: Checkout source
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🧩 Step 2: Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      # 🧩 Step 3: Install dependencies
      - name: Install dependencies
        run: npm ci --force

      # 🧩 Step 4: Build & Package SPFx Solution
      - name: Build & Package SPFx
        shell: pwsh
        run: |
          Write-Host "🧹 Cleaning and building SPFx solution..."
          $env:NODE_OPTIONS="--max_old_space_size=8192"
          npx gulp clean
          npx gulp build
          npx gulp bundle --ship
          npx gulp package-solution --ship

      # 🧩 Step 5: Verify required secrets
      - name: 🔍 Verify Required Secrets
        shell: pwsh
        env:
          M365_APP_ID: ${{ secrets.M365_APP_ID }}
          M365_TENANT_DOMAIN: ${{ secrets.M365_TENANT_DOMAIN }}
          M365_CERTIFICATE_BASE64: ${{ secrets.M365_CERTIFICATE_BASE64 }}
          M365_CERTIFICATE_PASSWORD: ${{ secrets.M365_CERTIFICATE_PASSWORD }}
          M365_APP_CATALOG_URL: ${{ secrets.M365_APP_CATALOG_URL }}
        run: |
          $required = @('M365_APP_ID','M365_TENANT_DOMAIN','M365_CERTIFICATE_BASE64','M365_CERTIFICATE_PASSWORD','M365_APP_CATALOG_URL')
          foreach ($s in $required) {
            if (-not (Test-Path "Env:\$s") -or [string]::IsNullOrEmpty((Get-Item "Env:\$s").Value)) {
              Write-Error "❌ Secret $s is missing or empty!"
              exit 1
            } else {
              Write-Host "✅ Secret $s found."
            }
          }

      # 🧩 Step 6: Deploy SPFx App (with optional debug)
      - name: 🚀 Deploy SPFx App (with optional debug)
        shell: pwsh
        env:
          M365_APP_ID: ${{ secrets.M365_APP_ID }}
          M365_TENANT_DOMAIN: ${{ secrets.M365_TENANT_DOMAIN }}
          M365_CERTIFICATE_BASE64: ${{ secrets.M365_CERTIFICATE_BASE64 }}
          M365_CERTIFICATE_PASSWORD: ${{ secrets.M365_CERTIFICATE_PASSWORD }}
          M365_APP_CATALOG_URL: ${{ secrets.M365_APP_CATALOG_URL }}
          DEBUG_MODE: ${{ github.event.inputs.debug }}
        run: |
          $transcriptPath = "C:\m365-debug.txt"

          # Enable debug transcript if requested
          if ($Env:DEBUG_MODE -eq 'true') {
            if (Test-Path $transcriptPath) { Remove-Item $transcriptPath -Force }
            Write-Host "🐞 DEBUG MODE ON — starting transcript to $transcriptPath"
            Start-Transcript -Path $transcriptPath -Force
          } else {
            Write-Host "🐞 DEBUG MODE OFF"
          }

          try {
            Write-Host "📦 Installing Microsoft 365 CLI..."
            npm install -g @pnp/cli-microsoft365@latest

            Write-Host "🧩 Verifying CLI installation..."
            m365 status
            m365 version

            Write-Host "🔐 Preparing certificate..."
            $certPath = "C:\cert.pfx"
            [IO.File]::WriteAllBytes($certPath, [Convert]::FromBase64String($Env:M365_CERTIFICATE_BASE64))
            if (-Not (Test-Path $certPath)) {
              Write-Error "❌ Certificate creation failed!"
              exit 1
            }

            Write-Host "🔑 Logging into Microsoft 365 using certificate authentication..."
            try {
              m365 login --authType certificate `
                --appId $Env:M365_APP_ID `
                --tenant $Env:M365_TENANT_DOMAIN `
                --certificateFile $certPath `
                --password $Env:M365_CERTIFICATE_PASSWORD
              Write-Host "✅ Microsoft 365 login successful."
            } catch {
              Write-Error "❌ M365 login failed: $($_.Exception.Message)"
              exit 1
            }

            Write-Host "🌐 Connecting to SharePoint App Catalog..."
            try {
              m365 spo set --authType certificate `
                --appId $Env:M365_APP_ID `
                --tenant $Env:M365_TENANT_DOMAIN `
                --certificateFile $certPath `
                --password $Env:M365_CERTIFICATE_PASSWORD `
                --url $Env:M365_APP_CATALOG_URL
              Write-Host "✅ Connection to App Catalog successful."
            } catch {
              Write-Error "❌ Failed to connect to App Catalog: $($_.Exception.Message)"
              exit 1
            }

            Write-Host "📁 Searching for .sppkg package..."
            $pkg = Get-ChildItem -Path $env:GITHUB_WORKSPACE -Recurse -Filter *.sppkg | Select-Object -First 1
            if (-not $pkg) {
              Write-Error "❌ No .sppkg file found in workspace: $env:GITHUB_WORKSPACE"
              exit 1
            }
            Write-Host "📦 Found package: $($pkg.FullName)"

            Write-Host "📤 Uploading and deploying SPFx app..."
            try {
              m365 spo app add --filePath "$($pkg.FullName)" --appCatalogUrl $Env:M365_APP_CATALOG_URL --overwrite
              m365 spo app deploy --name (Split-Path $pkg.FullName -Leaf) --appCatalogUrl $Env:M365_APP_CATALOG_URL --skipFeatureDeployment
              Write-Host "✅ SPFx App deployed successfully!"
            } catch {
              Write-Host "⚠️ Deployment failed: $($_.Exception.Message)"
              Write-Host "🔁 Retrying deployment once..."
              Start-Sleep -Seconds 10
              try {
                m365 spo app deploy --name (Split-Path $pkg.FullName -Leaf) --appCatalogUrl $Env:M365_APP_CATALOG_URL --skipFeatureDeployment
                Write-Host "✅ SPFx App deployed successfully on retry!"
              } catch {
                Write-Error "❌ Deployment failed after retry: $($_.Exception.Message)"
                exit 1
              }
            }

          } finally {
            if ($Env:DEBUG_MODE -eq 'true') {
              Write-Host "🛑 Stopping transcript..."
              try { Stop-Transcript } catch { Write-Warning "Stop-Transcript failed: $($_.Exception.Message)" }
              Write-Host "📄 Transcript saved to $transcriptPath"
            }

            if (Test-Path $certPath) {
              try { Remove-Item $certPath -Force -ErrorAction SilentlyContinue } catch { Write-Warning "⚠️ Could not remove certificate file: $($_.Exception.Message)" }
            }
          }

      # 🧩 Step 7: Upload transcript (always run)
      - name: 📤 Upload m365 Debug Transcript (if exists)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m365-debug-logs
          path: C:\m365-debug.txt
          if-no-files-found: ignore
